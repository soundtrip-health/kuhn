# Agent Backend Configuration
PORT=3002

# Postgres (matches docker-compose.yml defaults)
PGHOST=localhost
PGPORT=5432
PGDATABASE=kuhn
PGUSER=kuhn
PGPASSWORD=kuhn_dev

# CORS — allowed origin(s) for the TeXlyre dev server
CORS_ORIGIN=http://localhost:5173
