# Agent Backend Configuration
PORT=3002

# Postgres (matches docker-compose.yml defaults)
PGHOST=localhost
PGPORT=5432
PGDATABASE=kuhn
PGUSER=kuhn
PGPASSWORD=kuhn_dev

# CORS — allowed origins (comma-separated): webapp dev server is pinned to 5174
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
